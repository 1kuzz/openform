# Конструктор форм-конфигураторов ноутбуков

Этот проект представляет собой **визуальный конструктор форм**, позволяющий создавать многоразделные конфигурационные формы (например, для описания ноутбуков) и экспортировать их в формате **JSON**, совместимом с внешними системами, CMS, Google Apps Script и т.д.

Вы можете добавлять секции, поля, настраивать типы, валидацию, варианты выбора, подсказки, а затем просматривать форму так, как увидит конечный пользователь.

## ✨ Основные возможности

* 🧱 **Визуальный конструктор** разделов и вопросов
* 🔧 Поддержка типов полей: text, number, select, multiselect, checkbox, textarea, radio
* 📦 **Автогенерация ключей (name)** + проверка дубликатов
* 🔍 **Lint-проверка** конфигурации
* 👁 **Превью режим** (форма или компактная схема)
* 🌐 RU/EN подписи, независимые для каждой строки
* 💾 Локальное сохранение проекта (localStorage)
* ⬇ Экспорт готовой формы в JSON
* ⬆ Импорт JSON обратно в конструктор
* 🎨 Удобный интерфейс с табами, подсветкой ошибок и навигацией
* ⚠ Проверка корректности регулярных выражений и валидации

## 📁 Файлы проекта

| Файл                          | Описание                                                                                   |
| ----------------------------- | ------------------------------------------------------------------------------------------ |
| `form_configurator19_11.html` | Основное веб-приложение: конструктор, превью, экспорт, lint, UI-логика.                    |
| `config_laptop.json`          | Пример большой формы для конфигурации ноутбуков: разделы, поля, типы, валидация, варианты. |

## 🚀 Запуск

Приложение не требует сервера — оно работает полностью **локально**.

1. Скачайте HTML-файл:

   ```
   form_configurator19_11.html
   ```
2. Откройте его в браузере (Chrome / Edge / Firefox).
3. Начните добавлять секции, поля и настраивать свою форму.

## 🔄 Импорт и экспорт JSON

### Экспорт текущей формы

Кнопка:
**Превью → “Скачать JSON”**

Будет сохранён файл с полной структурой:

* разделы
* поля
* типы
* опции
* валидация
* переводы RU/EN

### Импорт структуры

Кнопка:
**Превью → “Загрузить JSON”**

После выбора файла редактор восстановит весь проект.

### Пример импорта

Формат схемы идентичен примеру из `config_laptop.json`
(файл из репозитория) .

## 🧩 Пример использования JSON (интеграция)

JSON, созданный приложением, можно использовать:

* для создания Google Forms через Apps Script
* для автозаполнения CRM-систем
* как часть конфигураторов устройств
* для внутренней документации оборудования
* как схему валидации для backend-форм

## 🧰 Функциональность редактора

Редактор поддерживает:

* Подсветку всех полей, где есть ошибки
* Переход к ошибке по клику в разделе «Проверка»
* Автосохранение (и ручное тоже)
* Переключение между простым и расширенным режимом
* Управление отображением полей EN-переводов
* Режим компактной схемы (для больших форм)
* Защиту от невалидных JSON-паттернов

## 📘 Структура формата JSON

Каждый раздел:

```json
{
  "id": "s_1",
  "title": "Материалы",
  "fields": [ ... ]
}
```

Каждое поле:

```json
{
  "id": "f_63",
  "name": "material",
  "labels": {
    "ru": "Укажите материалы корпуса...",
    "en": ""
  },
  "type": "select",
  "options": "PPPP, APPP, MPPP",
  "required": true,
  "hint": "",
  "validation": { "min": "", "max": "", "step": "", "pattern": "", "format": "" }
}
```

Полностью совместимо с примером в `config_laptop.json`.

## 🧪 Предпросмотр формы

Режимы:

* **Пользовательская форма** — визуально, как Google Forms
* **Схема** — компактно, как документация API

Можно сразу собрать объект ответов.

## 💾 Сохранение проекта

В браузере доступна кнопка:

**💾 Сохранить**

Этот проект будет доступен при следующем открытии страницы.

## 🛠 Технологии

* Чистый **HTML + CSS + JS**
* Без библиотек и без backend
* Вся логика происходит на стороне клиента

## 🌟 Планы на развитие

* Экспорт в Google Forms API
* Генерация UI под Bootstrap / Tailwind
* Автоматическая проверка логических зависимостей
* Тёмная тема

## 📄 Лицензия

MIT — делайте с проектом всё, что хотите ❤️
